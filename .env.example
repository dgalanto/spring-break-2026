# Server port
PORT=3000

# GitHub-backed comments
# REQUIRED: Create a Personal Access Token (classic) at https://github.com/settings/tokens
# with "repo" or at minimum "public_repo" scope for contents API access.
# Copy .env.example to .env and add your token (do NOT commit .env with your token!)
GITHUB_TOKEN=            # required: token with repo/contents scope (do NOT commit)
GITHUB_REPO=dgalanto/spring-break-2026
GITHUB_BRANCH=main
COMMENTS_PATH=data/comments.json

# Gemini / Vertex
GEMINI_API_URL=
GEMINI_API_KEY=
# To use Google auth (service account) set GOOGLE_APPLICATION_CREDENTIALS and:
# GEMINI_USE_OAUTH=true
GEMINI_USE_OAUTH=false
