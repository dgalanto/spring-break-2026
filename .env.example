

# Copy to .env and fill in values (do NOT commit .env with real keys)
GOOGLE_API_KEY=your_google_api_key_here
GOOGLE_CSE_ID=your_custom_search_engine_id_here
PORT=3000
# Optional: restrict CORS during development (set to front-end origin)
CORS_ORIGIN=http://localhost:5500


# Notes:
# - To run locally: copy .env.example to .env and set GITHUB_TOKEN
# - Start server: npm install && npm run dev
# - Quick curl tests:
#   curl -i http://localhost:3000/api/comments
#   curl -i -X POST http://localhost:3000/api/comments -H "Content-Type: application/json" -d '{"name":"test","text":"hello"}'
#   curl -i http://localhost:3000/api/comments/init
